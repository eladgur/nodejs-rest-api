version: '3.1'

services:
  app:
    build: .
    ports: 
      - 3000:3000
    depends_on: 
      - db
  db:
    image: mongo
    restart: always
    ports:
      - 27017:27017
    environment:
      DB_USERNAME: mongo
      DB_PASSWORD: mongo